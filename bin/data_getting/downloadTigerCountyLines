#!/bin/bash

set -e

cd "$( dirname "${BASH_SOURCE[0]}" )"

DATA_DIR=../../data/tiger
mkdir -p "$DATA_DIR"

curl 'https://www2.census.gov/geo/tiger/TIGER2018/COUNTY/tl_2018_us_county.zip' > "$DATA_DIR/tl_2018_us_county.zip"
