#!/bin/bash

set -e
set -a

# Change CWD to this script's directory.
pushd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null

# source the database connection config.
. ../.env

pg_dump --clean --if-exists --no-owner > ./sql/entireDatabase.sql

popd >/dev/null
